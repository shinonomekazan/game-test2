// Firebase project configuration for ranking sharing
// Replace each value with your Firebase project's settings.
// Steps to obtain these values:
//   1. Go to https://console.firebase.google.com/
//   2. Create (or open) your project
//   3. Click the gear icon → "Project settings"
//   4. Under "Your apps", add a Web app (</> icon) and copy the config object below
//   5. In Firestore, create a database and set rules to allow read/write (see README)
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT_ID.appspot.com",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID"
};
